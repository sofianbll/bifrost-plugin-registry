# Domain docs

This is a single-context repo. Before changing domain behaviour, read the root `CONTEXT.md` and relevant `docs/adr/` files if they exist. `/domain-modeling` creates these files when terms or decisions need recording; their absence is normal.

Use terms from `CONTEXT.md` in issues, code and tests. Surface a conflict with an ADR before changing the recorded decision.
